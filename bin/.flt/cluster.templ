kind: Cluster
metadata:
  name: {{cluster}}
  labels:
    environment: dev
    region: {{region}}
    zone: {{zone}}
    district: {{district}}
    store: {{cluster}}
    domain: {{cluster}}.{{domain}}
    gitopsRepo: {{repo}}
    gitopsBranch: {{branch}}
spec:
