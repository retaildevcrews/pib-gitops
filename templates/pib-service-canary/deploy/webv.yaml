apiVersion: apps/v1
kind: Deployment
metadata:
  name: webv-{{coral.app}}
  namespace: {{coral.namespace}}
  labels:
    app.kubernetes.io/name: webv-{{coral.app}}
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
       maxSurge: 1
       maxUnavailable: 1
  selector:
    matchLabels:
      app: webv-{{coral.app}}
  template:
    metadata:
      labels:
        app: webv-{{coral.app}}
    spec:
      containers:
        - name: app
          image: ghcr.io/retaildevcrews/webv-red:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            failureThreshold: 10
            periodSeconds: 60
          startupProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            failureThreshold: 60
            periodSeconds: 2
          resources:
            limits:
              memory: "256Mi"
              cpu: "500m"
            requests:
              memory: "128Mi"
              cpu: "250m"
          args:
          - --sleep
          - "1000"
          - --prometheus
          - --run-loop
          - --server
          - http://central-tx-hou-100-fleet1.cseretail.com
          - http://central-tx-hou-101-fleet1.cseretail.com
          - --files
          - imdb-benchmark.json
          - --zone
          - dev
          - --region
          - dev
          - --log-format
          - Json

---
apiVersion: v1
kind: Service
metadata:
  name: webv-{{coral.app}}
  namespace: {{coral.namespace}}
  labels:
    app: webv-{{coral.app}}
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: webv-{{coral.app}}
