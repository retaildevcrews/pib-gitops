template: .gitops
deployments:
  dev:
    target: dev
    values:
      environment: dev
      name: flagger-grafana
      namespace: istio-system
      subpath: /grafana
      targets: []
      workspace: istio-system
