template: pib-service-canary
deployments:
    dev:
        target: dev
        values:
            app: myapp
            environment: dev
            image: ghcr.io/retaildevcrews/myapp:bad
            name: myapp
            namespace: myapp
            port: 8080
            targets:
                - all
            workspace: myapp
